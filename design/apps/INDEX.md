# INDEX — apps → phases

Per-app phase list. Links to each app's board + phase specs. See `PROGRESS.md` for the roll-up.

| App | Phases | Board |
|-----|--------|-------|
| _example_ | scaffold → features → query-perf → integration-hardening → frontend-wiring → flutter-wiring → extension-wiring | `<app>/README.md` |

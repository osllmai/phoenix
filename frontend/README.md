# Phoenix Frontend

Next.js 15 (App Router) + TypeScript + TanStack Query (mirrors `health` web).

**Role:** optional web surface for Phoenix; talks to the backend at `/api/v1`.
Scaffold only (created as part of the full monorepo). Dev: `pnpm dev` → :3000.

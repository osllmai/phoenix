# === Build output (Flutter desktop + llama.cpp engine still emit CMake artifacts) ===
build/
cmake-build*/
CMakeFiles/
CMakeCache.txt

# === Compiled binaries / objects (C/C++ engine) ===
*.o
*.obj
*.so
*.a
*.lib
*.dll
*.db
*.out

# === Debug and temporary files ===
*.log
*.tmp
*.cache
*.bak
*~

# === OS generated files ===
.DS_Store
Thumbs.db
ehthumbs.db
desktop.ini

# === Editor settings ===
.vscode/

# === Models (Whisper / local GGUF) ===
*.bin
*.ggml
ggml-*

# === Python / Django ===
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.sqlite3
staticfiles/
media/

# === Dart / Flutter tooling (build cache + deps — never commit) ===
.dart_tool/
.pub-cache/
.pub/
.flutter-plugins
.flutter-plugins-dependencies

# === Node / Next.js / pnpm / WXT extension (web + extension surfaces) ===
node_modules/
.pnpm-store/
.next/
out/
.turbo/
.output/
.wxt/
*.tsbuildinfo
next-env.d.ts
coverage/
test-results/

# === Flutter native (iOS / Android) ===
Pods/
.symlinks/
**/Flutter/ephemeral/
local.properties
.gradle/

# === IDE (JetBrains) ===
.idea/
*.iml

# === Secrets / local env ===
**/apikey.h
.env
.env.*
!.env.example

# === Agent config / generated docs ===
.claude
.claude/*
CLAUDE.md
# Design folder is local-only — never push to GitHub
design/
*/design/*

# Backend is local-only — not shared
backend/

mobile/test/*
import QtQuick 2.15

QtObject {
    enum Menu{
        PageMenu,
        ButtonMenu
    }

    enum BottonType{
        Primary,
        Danger
    }

    enum State{
        Normal,
        Hover,
        Pressed,
        Disabled,
        Selected
    }
}

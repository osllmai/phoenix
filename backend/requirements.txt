# Phoenix backend — Django + django-ninja + Celery (mirrors health stack)
Django>=5.0,<5.2
django-ninja>=1.3
# Canonical auth core — allauth headless (owns hashing/sessions/verify/reset/MFA;
# no hand-rolled auth, no paid SaaS). [mfa] pulls qrcode+fido2.
django-allauth[mfa,socialaccount]>=65.18,<66.0
argon2-cffi>=23.1
celery>=5.4
redis>=5.0
psycopg[binary]>=3.2
django-environ>=0.11
gunicorn>=23.0
pytest>=8.0
pytest-django>=4.8
pytest-cov>=5.0
ruff>=0.6
